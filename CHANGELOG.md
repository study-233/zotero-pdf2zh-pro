# 更新记录

这里记录 `zotero-pdf2zh-pro` 的用户可见变化。旧项目的历史仍保留在 Git 历史中，
不作为本产品的版本序列。

## Unreleased

- 精简后续远端发布验证：复用同一提交的核心 CI，默认执行 Windows 打包和安装生命周期基础验证；OCR、回滚、迁移等扩展检查保留手动开启。PyPI 与 GitHub 继续复用已验证产物。

## v1.6.9 - 2026-09-12

- 正文定向复核改为默认关闭，保留手动开启入口；升级时一次性关闭旧开关，之后保留用户主动选择。历史任务的重试和补译仍沿用原任务设置。
- 修复任务关联恢复、迟到响应覆盖、SSE 断线同步、同名输出导入隔离及重复导入竞态；任务显式 FIFO 排队，排队和运行取消更可靠。
- 段落恢复记录改为 SQLite 增量保存，兼容旧 JSON；批次部分失败只补失败段，补齐最终批次缓存，减少重复翻译请求。
- 改善有几何和词语证据的正文空格、断词及部分跨栏衔接；新增 CSV 术语表导入、替换、清除，并随任务保存词表快照。自动术语抽取仍默认关闭。
- 关闭自动术语抽取时复用主翻译器；分别统计初始化、翻译和复核请求、推理 tokens 与阶段耗时。修复 Docker 镜像遗漏服务模块。
- 已知限制：手动复核可能显著增加耗时，且不保证语义正确；部分术语、跨栏句法、行内公式及旋转图中文字仍需改进。本版不承诺整体加速比例。

## v1.6.8 - 2026-09-09

- 修复 Windows 控制台卸载时 `purgeData` 参数类型错误；等待确认框返回后再执行，明确提供保留或删除数据选项，并修正升级、更新和迁移的异步确认。

## v1.6.7 - 2026-09-09

- 修正 Windows PowerShell 5.1 下脚本中文编码，确保中文提示正确显示；卸载快捷方式统一使用 `Uninstall`，避免英文系统保存中文快捷方式失败；发布验证默认只保留启动、安装和升级检查，完整 OCR、回滚及迁移测试改为手动选项。
- 修复 Windows 新电脑双击控制中心时提示缺少 `WebView2Loader.dll` 的问题：正式版本固定使用 MSVC x64 构建并静态链接加载器及 C/C++ 运行库。
- 首次打开时自动检测并联网安装缺失的 Microsoft Edge WebView2 Runtime，验证微软数字签名，显示原生准备窗口，并支持失败重试和查看日志；登录自启时不自动弹出安装窗口。
- 修正 WebView2 Runtime 注册表标识，覆盖当前用户和整机安装；发布验证增加普通/延迟 DLL 导入检查及最终 ZIP 的真实窗口启动测试。
- 已遇到启动报错的用户请下载新版 Windows ZIP，完整解压后运行其中的 EXE，按界面完成安装或升级，无需先卸载原版。

## v1.6.6 - 2026-09-08

- 修复 Windows 安装目录迁移在提交注册表、快捷方式或自启状态时偶发错误 122 的问题；仅对此错误自动重试，并在失败回滚时保留具体系统错误与调用位置。
- 迁移验证现在覆盖私有 uv 缓存和快捷方式目标，并为错误 122 的重试与非重试错误增加快速测试。

## v1.6.5 - 2026-09-08

- 修复 Windows PowerShell 5.1 下自更新和安装目录迁移实际成功后仍可能被发布验证误报失败的问题，并在异常时保留完整诊断输出。
- 修复更改 Windows 安装位置后，迁移进程退出可能导致原本运行中的翻译服务停止的问题。

## v1.6.4 - 2026-09-08

- Windows 安装日志不再把 uv 的普通 stderr 进度逐行标记为错误，自动更新窗口会显示真实安装阶段和命令输出。
- Windows 首次安装支持选择完整产品目录，控制中心、私有 uv、Python、缓存、数据及日志可放到其他磁盘；已有安装支持带健康验证和失败回滚的整体迁移。

## v1.6.3 - 2026-09-08

- 翻译配置统一为按名称和模型选择，取消服务类型与激活状态的双重选择；自动备份并迁移旧配置，同一批任务固定使用提交时的配置。
- 简化 OpenAI 兼容中转站编辑，新增获取模型、搜索与键盘选择、测试未保存配置和“保存并使用”；密钥默认隐藏，配置管理支持复制，运行参数默认折叠。
- 本地服务新增 `/list-models`，保留自定义路径，查询失败可手填模型；区分本地服务连接状态和真实 API 测试结果。

- 服务启动时统一启用系统信任证书，修复 Reqable 等已受系统信任的 HTTPS 代理触发 TLS 错误，以及重装服务覆盖启动脚本后修复失效的问题；保留证书与域名验证。
- 修复纯网址脚注被误判为漏译、反复补译仍失败的问题；支持脚注编号、多个网址和依据 PDF 行结构识别的折行路径，保留网址原样并跳过翻译请求。

## v1.6.2 - 2026-09-07

- 修复段落翻译失败后仍误报完成的问题：新增“未完成”状态、失败段落详情，并阻止未完成译文自动导入 Zotero。
- 连接错误、超时及临时服务错误统一最多尝试 5 次，重试遵守限流、并发上限与 Retry-After，等待期间支持取消。
- 校验批量 JSON、段落 ID、空译文、原文回显及占位符；无效结果不再写入缓存，读取旧缓存时定点清理坏结果。
- 新增“补译”及历史任务检查补译，持久化已验证译文和失败记录；服务重启后继续恢复，默认以 QPS 2、并发 4 补译未完成段落。
- 修复批量失败转单段翻译时的正文赋值问题；完整补译后生成新附件并防止重复导入，保留已有附件。

## v1.6.1 - 2026-09-07

- 修复 Zotero 深色主题下 LLM API 编辑器的高级设置标题不可见，并统一接口协议与请求参数控件的显示样式。
- 增强 OpenAI 兼容中转站的协议自动识别，支持本地化错误和协议错误码，同时避免因鉴权、模型或参数错误误切换协议。
- 连接检查直接返回已解析协议，不再重复探测；为 TLS 证书失败和客户端受限的 403 响应提供不泄露密钥的明确诊断。

## v1.6.0 - 2026-09-06

- OpenAI 兼容入口新增 Chat Completions 与 Responses 双协议，支持自动识别和手动指定，正文翻译、连接检查与术语提取使用同一适配层。
- 支持完整接口地址和中转站路径前缀，新增 requestOptions JSON 高级参数；识别成功后在任务内固定协议，按实际协议、地址和参数隔离缓存。
- 通用请求指标覆盖耗时、QPS、重试、本地及服务端缓存、token 和吞吐量，缺失统计显示不可用；移除 DeepSeek 计费、价格清单与后台更新，保留 DeepSeek 翻译。
- 插件检查服务端协议能力，旧服务端继续兼容 Chat 配置，Responses 配置会明确提示升级；Python 服务要求 OpenAI SDK 2.32.0 或更新版本。
- 新增按版本与完整提交 SHA 手动触发的 Windows 发布构建，验证控制中心、安装升级回滚及 Python 3.13 OCR，并保留源码和产物校验清单。

## v1.5.0 - 2026-09-02

- 默认关闭自动术语提取并将翻译线程池提高到 50，避免长文档在术语阶段额外消耗大量 token，同时提升 DeepSeek 翻译吞吐。
- DeepSeek 术语提取在手动开启时使用独立的非思考请求，不影响正文翻译的思考模式，并隔离对应缓存命名空间。
- 修正 DeepSeek 高峰价仅适用于北京时间工作日，补充 Flash Vision 费率，并显示费率版本、来源与更新时间。
- 新增经严格校验的 DeepSeek 定价清单、24 小时后台刷新和每日官网检查；检测到官方调价时自动创建审核 PR，网络失败时继续使用最后有效或内置规则。

## v1.4.0 - 2026-09-01

- Windows 控制中心新增稳定版静默检查和一键自动更新，下载后校验官方 Release 地址、大小、SHA-256、ZIP 路径与包内版本，再安全替换控制中心和 PyPI 服务端；失败时继续回滚旧版。

## v1.3.0 - 2026-09-01

- 日常 GitHub CI 精简为单一 Ubuntu 核心任务，文档修改不再触发测试，Windows 完整打包仅在发布时执行。
- 服务端测试合并为 25 个核心行为契约，移除重复的字段映射、包装路由和打包策略测试。
- 发布与本地部署不再重复运行单测、lint 和全新环境冒烟，保留实际构建、产物校验、健康检查与回滚。

## v1.2.0 - 2026-08-31

- Windows ZIP 新增 Tauri 2 单卡片控制中心，用户双击 EXE 即可完成安装、启动、停止、状态查看、日志和数据目录访问。
- 新增当前用户登录自启、系统托盘和单实例支持；自启时静默确保服务运行，退出控制中心不会停止服务。
- 新增安全升级、降级阻止、失败回滚和可选保留数据卸载，并继续校验服务 PID、命令行、可执行文件路径和健康接口归属，不结束未知端口进程。
- 安装过程实时显示并保存日志，缺少 WebView2 时给出原生提示和官方安装入口；PowerShell/CMD 工具继续随包提供故障恢复能力。
- CI 新增前端与 Rust 测试、Tauri release 构建、Windows ZIP 内容校验和完整 Windows 安装升级生命周期测试。

## v1.1.0 - 2026-08-31

- 新增 macOS 本机源码一键部署工作流，支持插件与后端构建、制品校验、任务保护、Homebrew 常驻更新、备份和失败回滚。
- DeepSeek 任务新增请求耗时、活跃请求、实际 QPS、缓存命中、自动重试、token、估算费用、段落吞吐量和预计剩余时间，并在任务面板提供可展开详情。
- 翻译缓存新增 provider、端点、模型、语言和提示词指纹命名空间，避免配置变化后错误复用；指标日志与服务跟踪不记录正文、译文、完整提示词或 API Key。
- 偏好页新增默认关闭的“不翻译参考文献”，支持版面标签和保守的参考文献标题识别，输出 PDF 保留参考文献原文。
- 翻译完成并成功导入 Zotero 后，可按偏好设置发送系统通知。

## v1.0.1 - 2026-08-31

- 将插件兼容范围更新为 Zotero 8.0 至 10.0.x，并新增源码、构建目录与 XPI 清单的一致性测试。
- Windows 安装包改用 ASCII 文件名，避免不同系统区域设置下的解压和启动问题。
- 修复 uv 启动器使用 Python 子进程监听端口时的 Windows 服务归属判断。
- 仓库改为公开，并通过 Release 中的 `update.json` 为 Zotero 插件提供自动更新。
- GitHub Release 发布 XPI、自动更新清单与 Windows ZIP，不再构建或上传朋友版制品。

## v1.0.0 - 2026-08-31

- 以独立产品 `zotero-pdf2zh-pro` 发布，使用新的插件 ID、设置空间、CLI、数据目录和发行物名称。
- 服务端发布到由 `study-233` 控制的新 PyPI 项目，不再安装旧开发者拥有的发行包。
- 新增 Windows 10/11 x64 一键安装、手动启停、日志查看和一键卸载，并明确不注册自启动。
- GitHub Release 提供 XPI 和 Windows 安装包；向无仓库权限的接收者分发时单独提供对应源码。
- 保留固定的 pdf2zh-next、BabelDOC 和 RapidOCR 核心快照及全部第三方许可证声明。
