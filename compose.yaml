services:
  zotero-pdf2zh-pro:
    build:
      context: .
      dockerfile: server/Dockerfile
      args:
        UV_INDEX_URL: "${UV_INDEX_URL:-}"
    image: "${PDF2ZH_IMAGE:-zotero-pdf2zh-pro:local}"
    ports:
      - "${PDF2ZH_PORT:-8890}:8890"
    volumes:
      - "${PDF2ZH_TRANSLATES_DIR:-./server/translates}:/app/server/translates"
      - "${PDF2ZH_BABELDOC_CACHE_DIR:-./server/cache/babeldoc}:/root/.cache/babeldoc"
      - "${PDF2ZH_NEXT_CACHE_DIR:-./server/cache/pdf2zh_next}:/root/.cache/pdf2zh_next"
    environment:
      PDF2ZH_LOG_LEVEL: "${PDF2ZH_LOG_LEVEL:-INFO}"
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "from urllib.request import urlopen; urlopen('http://127.0.0.1:8890/health').read()",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
