{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The control center only uses its own typed Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
